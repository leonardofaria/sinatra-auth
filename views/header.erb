<header class="flex items-center justify-between bg-gray-800 p-6 w-full mb-10">
  <h1 class="text-4xl text-gray-200"><%= ENV['WEBSITE_NAME'] %></h1>

  <% if authenticated? %>
    <a class="text-gray-600 no-underline hover:text-gray-200 hover:text-underline py-2 px-4" href='/logout'>Sign out (<%= @current_user.username %>)</a>
  <% end %>
</header>
